import core.io.InputHandler;

public class Main {
    public static void main(String[] args) {
        // InputHandler keeps the user in a loop until they exit the program via 'exit'.
        InputHandler inputHandler = new InputHandler(System.in);
    }
}
